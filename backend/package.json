{
  "name": "tedxguc-hub-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "db:reset": "node -e \"import('node:fs').then(fs=>{const p='data/tedxguc.sqlite'; if(fs.existsSync(p)){fs.unlinkSync(p); console.log('Deleted', p);} else {console.log('No database file found.');}})\"",
    "dev": "node index.js",
    "start": "node index.js",
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^6.10.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^12.9.0"
  },
  "engines": {
    "node": "20.x"
  },

    "refresh-data": "npm run db:reset && npm run dev"
  
 
}
