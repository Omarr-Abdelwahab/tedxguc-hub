{
  "name": "tedxguc-hub-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "db:reset": "node scripts/reset-db.js",
    "db:export": "node ../scripts/export-db-to-json.js",
    "dev": "node index.js",
    "start": "node index.js",
    "test": "node --test",
    "refresh-data": "npm run db:reset && npm run dev"
  },
  "dependencies": {
    "nodemailer": "^6.10.0"
  },
  "optionalDependencies": {
    "better-sqlite3": "^12.9.0"
  },
  "engines": {
    "node": "20.x"
  }
}
